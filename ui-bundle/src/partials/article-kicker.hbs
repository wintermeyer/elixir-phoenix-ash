{{! Mobile-only strip that hosts the sidebar toggle. The back-link
    to the book root was dropped: it duplicated information already
    carried by the top nav and the left sidebar on every page. }}
<div class="article-kicker">
{{> nav-toggle}}
</div>
