site:
  title: Phoenix Framework Beginner's Guide
  # the 404 page and sitemap files only get generated when the url property is set
  url: https://www.wintermeyer-consulting.de/books/phoenix
  start_page: book::index.adoc
content:
  sources:
  - url: git@github.com:wintermeyer/phoenix-book.git
    branches: master
    # setting edit_url to false disables the Edit this Page link for any page that originates from this repository
    # the same thing can be achieved by adding empty credentials (i.e., @) in front of the domain in the URL
    edit_url: false
ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true
  supplemental_files: ./supplemental-ui
runtime:
  fetch: true